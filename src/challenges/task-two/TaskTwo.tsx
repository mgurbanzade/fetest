import TaskTwoWorkspace from './TaskTwoWorkspace'

const TaskTwo: React.FC = () => {
  return (
    <section>
      <h2>Timeline Helpers</h2>
      <p>Implement the two helper functions inside the workspace (grouping and risk summary). Afterwards, discuss optimisation options or test coverage ideas.</p>
      <TaskTwoWorkspace />
    </section>
  )
}

export default TaskTwo

