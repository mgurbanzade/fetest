import TaskOneWorkspace from './TaskOneWorkspace'

const TaskOne: React.FC = () => {
  return (
    <section>
      <h2>Backlog Filters</h2>
      <p>Implement two helper functions in the workspace so the list and summary render correctly. Once they work, talk through possible optimisations or tests.</p>
      <TaskOneWorkspace />
    </section>
  )
}

export default TaskOne

